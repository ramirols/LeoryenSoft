---
const {
    title = "Contáctame y hagamos realidad tu proyecto",
    subtitle = "Cuéntame sobre tu idea, negocio o necesidad digital. Te responderé en menos de 24 horas.",
} = Astro.props;
---

<section id="contacto" class="relative max-w-3xl mx-auto px-6 py-20">
    <!-- Header -->
    <header class="text-center mb-12 animate-fade-up">
        <h2 class="text-3xl md:text-4xl font-bold mb-4">
            {title}
        </h2>
        <p class="text-gray-600 max-w-xl mx-auto">
            {subtitle}
        </p>
    </header>

    <!-- Form -->
    <form
        action="https://formspree.io/f/your-form-id"
        method="POST"
        class="space-y-6 bg-white shadow-2xl p-8 md:p-10 rounded-3xl border animate-fade-up animation-delay-200"
    >
        <!-- Nombre -->
        <div>
            <label for="name" class="block text-sm font-medium mb-1">
                Nombre completo
            </label>
            <input
                id="name"
                name="name"
                type="text"
                required
                minlength="3"
                placeholder="Ej. Juan Pérez"
                class="w-full rounded-xl border px-4 py-3 transition focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500"
            />
        </div>

        <!-- Email -->
        <div>
            <label for="email" class="block text-sm font-medium mb-1">
                Correo electrónico
            </label>
            <input
                id="email"
                name="email"
                type="email"
                required
                placeholder="correo@empresa.com"
                class="w-full rounded-xl border px-4 py-3 transition focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500"
            />
        </div>

        <!-- Mensaje -->
        <div>
            <label for="message" class="block text-sm font-medium mb-1">
                Cuéntame sobre tu proyecto
            </label>
            <textarea
                id="message"
                name="message"
                rows="5"
                required
                minlength="10"
                placeholder="Necesito una página web, sistema, tienda online, etc."
                class="w-full rounded-xl border px-4 py-3 transition focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500"
            ></textarea>
            <p class="text-xs text-gray-500 mt-1">
                Mientras más detalles, mejor podré ayudarte 🚀
            </p>
        </div>

        <!-- CTA -->
        <button
            type="submit"
            class="group relative w-full overflow-hidden rounded-xl bg-blue-600 py-3 font-semibold text-white transition hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-500"
        >
            <span class="relative z-10">Enviar mensaje</span>
            <span
                class="absolute inset-0 -translate-x-full bg-blue-700 transition-transform duration-300 group-hover:translate-x-0"
            ></span>
        </button>

        <!-- Trust -->
        <p class="text-center text-xs text-gray-500 pt-2">
            🔒 Tu información es confidencial. No spam, solo contacto directo.
        </p>
    </form>
</section>

<style>
    @keyframes fade-up {
        from {
            opacity: 0;
            transform: translateY(24px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    .animate-fade-up {
        animation: fade-up 0.6s ease forwards;
    }

    .animation-delay-200 {
        animation-delay: 0.2s;
    }
</style>
